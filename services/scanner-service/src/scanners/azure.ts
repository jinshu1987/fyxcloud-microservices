/**
 * Azure Scanner — copy the contents of server/azure-scanner.ts here.
 */
export async function scanAzureAccount(creds: any): Promise<{ assets: any[]; models: any[]; errors: string[] }> {
  return { assets: [], models: [], errors: [] };
}
