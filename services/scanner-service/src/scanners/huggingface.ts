/**
 * HuggingFace Scanner — copy the contents of server/hf-scanner.ts here.
 */
export async function scanHuggingFaceAccount(creds: any): Promise<{ assets: any[]; models: any[]; errors: string[] }> {
  return { assets: [], models: [], errors: [] };
}
