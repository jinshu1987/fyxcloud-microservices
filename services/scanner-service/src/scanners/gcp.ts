/**
 * GCP Scanner — copy the contents of server/gcp-scanner.ts here.
 */
export async function scanGcpAccount(creds: any): Promise<{ assets: any[]; models: any[]; errors: string[] }> {
  return { assets: [], models: [], errors: [] };
}
